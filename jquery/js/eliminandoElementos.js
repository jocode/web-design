$(document).ready(function(){

	// remove() ->  Elimina los el elemento con todos sus hijos
	// $('#contenedor').remove();

	// Eliminar elementos hijos
	// $('#contenedor').children('#segunda').remove();

	// Elimina todos los elementos nodo hijos
	$('#contenedor').empty();

});